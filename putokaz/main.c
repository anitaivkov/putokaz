#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include "header.h"

//koncepti: typedef u funkcijama i strukturama(3.), snake_case(4.), STATIC!!(5.),
//extern(7.), makro i inline fcije(8.), const(12.), staticka polja(13.),
//fseek itd (18.), (19.), upravljanje errorima (20.),  (23.), def jednostruko povezana lista

//biljeske: koristi const!, staticko polje, malloc != calloc, mozda implementirati 
//pravu rekurziju


int main() {
	main_menu();

	return 0;
}